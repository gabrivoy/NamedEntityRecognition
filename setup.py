from distutils.core import setup
from setuptools import find_packages

setup(
    name="flask-app-tdd",
    version="0.0.1",
    description="A simple Named Entity Recognition API",
    packages=find_packages(),
)